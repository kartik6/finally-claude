services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      LLM_MOCK: "true"
      OPENROUTER_API_KEY: "test-key"
    volumes:
      - /app/db
